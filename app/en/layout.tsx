import { Metadata } from '@/hooks/useMetadata'

export const metadata = Metadata('Hue Festival 2025 – Converging and Spreading Heritage')
export default function RootLayout({
  children
}: Readonly<{
  children: React.ReactNode
}>) {
  return children
}
